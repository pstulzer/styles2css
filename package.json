{
  "name": "colormerge",
  "version": "1.0.0",
  "description": "Your Figma plugin",
  "main": "dist/code.js",
  "scripts": {
    "dev": "vite src",
    "clean": "rimraf dist",
    "build": "npm run clean && run-p build:prod build:code",
    "build:prod": "vite build src --watch",
    "build:code": "vite build src/figmacode --watch",
    "preview": "vite preview --open"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "color": "^4.2.3",
    "color-blend": "^3.0.1",
    "npm-run-all": "^4.1.5",
    "prismjs": "^1.23.0",
    "rimraf": "^3.0.2",
    "vite": "^3.0.0",
    "vite-plugin-singlefile": "^0.11.0"
  }
}
